{
  "name": "agent-quest",
  "private": true,
  "version": "0.1.0",
  "description": "A Zelda-style top-down control room for Claude Code agents",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run typecheck -w server && npm run build -w web",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.2",
    "concurrently": "^9.2.0"
  }
}
